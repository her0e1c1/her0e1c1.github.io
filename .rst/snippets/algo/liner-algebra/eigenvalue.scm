
(ps "固有値")
(math "n次正方行列Aの固有値aは、x $ne 0に対して、Ax=axを満たすxが存在するときのa.xを固有ベクトルと呼ぶ")

(ps "固有空間")

(math "aがAの固有値 $Leftrightarrow det(A-aE)=0")


(ps "対角化")
(math "Aが対角化可能 $Leftrightarrow n個の1次独立な固有ベクトルをもつ")
(math "n個の異なる固有値をもつ $rightarrow Aは対角化可能")

(ps "ケイリー・ハミルトンの定理")

(math "行列AとB対角化可能 $Leftrightarrow AB=BA")
