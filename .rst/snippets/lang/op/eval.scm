
(sh "eval \"echo $HOME\"")

;; def func(t):
;;     return 2 * t
;; with open("./rc.py") as f:
;;     exec(f.read() , locals())
;; print "a = {a}, b = {b}, c = {c}".format(**locals())

;; # percolは設定ファイルをexecで読み込んでいる
;; a = 1
;; b = "aiueo"
;; c = func(10)
