;; template<class T> inline
;; void swap(T &first, T &second)
;; {
;;   if (&first != &second)
;;     {
;;       T tmp = first;
;;       first = second;
;;       second = tmp;
;;     }
;; }

;; template<class T> inline
;; void extend(T &a, T &b) {
;;   a.insert(a.end(), b.begin(), b.end());
;; }
